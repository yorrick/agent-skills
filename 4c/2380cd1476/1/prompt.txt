You are working in /Users/yorrickjansen/work/claude-code-plugins. Execute Task 2 from the plan at /Users/yorrickjansen/work/claude-code-plugins/docs/superpowers/plans/2026-03-17-workflow-eval-framework.md.

Read the plan's Task 2 section carefully, then update dev-loop/commands/workflow.md:
1. Replace the script template (the ```python block under '## Script template') to use build_graph(models=None)
   The new template should have:
   - def build_graph(models: dict[str, bool] | None = None) -> StateGraph:
   - Inside: if models is None: models = detect_available_models()
   - HAS_CODEX and HAS_GEMINI derived from models param
   - Returns the graph
   - if __name__ == '__main__' block calls build_graph(), handles --diagram, runs graph
2. Update the 4 example workflows to wrap graph construction in build_graph().    For examples 1-3, just wrap in build_graph(). For example 4 (full pipeline),    also move HAS_CODEX/HAS_GEMINI inside build_graph().
3. Add to 'Important rules' section: '**Use `build_graph()`.** Always put graph    construction in a `build_graph(models=None)` function...'
4. Commit with message: 'feat(workflow): require build_graph() in generated scripts'